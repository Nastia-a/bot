version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: youtube-downloader-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./downloads:/app/downloads
